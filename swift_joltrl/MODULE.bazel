module(
    name = "swift_joltrl",
    version = "1.0.0",
)

# --- Bazel Rules ---
bazel_dep(name = "rules_cc", version = "0.2.17")
bazel_dep(name = "rules_swift", version = "1.18.0")
bazel_dep(name = "bazel_skylib", version = "1.7.1")

# --- External Physics Engine (Jolt) ---
# We point to the same Jolt source as the main project but redefine it here
# to make this workspace self-contained.
http_archive = use_repo_rule("@bazel_tools//tools/build_defs/repo:http.bzl", "http_archive")

http_archive(
    name = "jolt",
    build_file = "@swift_joltrl//:jolt.BUILD",
    strip_prefix = "JoltPhysics-5.0.0",
    url = "https://github.com/jrouwe/JoltPhysics/archive/refs/tags/v5.0.0.tar.gz",
)
